{
	"name": "slugify",
	"version": "1.0.4",
	"description": "Rename files to slug-case and automatically update links.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.0.0",
		"builtin-modules": "^3.3.0",
		"esbuild": "0.19.11",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "5.3.3"
	}
}
